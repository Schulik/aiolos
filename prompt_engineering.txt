Hi chatgpt. I am programming a hydrodynamic simulation in the programming language C++. My programm will use a simple, implicit algorithm to solve the Euler system of hydrodynamics in its conservative form, on a non-uniform grid of size "num_cells+2". The "+2" is due to boundary cells. 
The method I am using is that of a finite volume method, for all three components, of density, momentum and total energy. Further, my method uses the fact that implicit hydrodynamic problems of first order can be expressed as band-tridiagonal matrices. The tri-diagonal structure results in individual treatment of lower, diagonal and upper matrix parts, together with the "right" part of the matrix equation. 
Those sub-matrices are represented as linear arrays in my code, correspondingly named "ll, dd, uu" and "r". Because the hydrodynamic system solves for 3 variables, i.e. density, momentum and total energy, the size of the "r" matrix is "(num_cells+2)*3", written in the code as "(base->num_cells + 2) * num_vars ;". The ll,dd,uu matrices are larger, as they consist of "(num_cells+2)*3*3" entries, written in the code as " //Allocate matrices int num_vars = 3; // + num_species int stride = num_vars * num_vars ; int size_r = (base->num_cells + 2) * num_vars ; int size_M = (base->num_cells + 2) * stride ; std::vector<double> ll(size_M, 0.), dd(size_M, 0.), uu(size_M, 0.), r(size_r, 0.) ;" . 

Now I have written a code below, which acts as unity operator on all cells, for all three variables, which exists once per cell. I have successfully tested it, in that it does nothing, as a unity operator should. The code is: 

" /** * implicit.cpp * * This file contains routines solving the hydrodynamic euler system with source terms in an implicit fashion */ #include <iomanip> #include <sstream> #include <stdexcept> #include "aiolos.h" //~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ // // Incompressible // //~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ /** * Computes the implicit hydro solution for incompressible fluids - applicable to gases e.g. at low mach numbers for electrons * * @param sp: the species of which to compute the timestep advance */ void c_Species::implicit_incompressible(double dt) { cout<<" Hi i am species "<<speciesname<<" and i am attempting to be solved implicitly."<<endl; //Boundaries this->apply_boundary_left(this->u) ; this->apply_boundary_right(this->u) ; //Allocate matrices int num_vars = 3; // + num_species int stride = num_vars * num_vars ; int size_r = (base->num_cells + 2) * num_vars ; int size_M = (base->num_cells + 2) * stride ; std::vector<double> ll(size_M, 0.), dd(size_M, 0.), uu(size_M, 0.), r(size_r, 0.) ; //Write into matrices for (int j=0; j < num_cells; j++) { cout<<" writing matrix j "<<j<<endl; int idx = j*stride ; int idx_r = j*num_vars; // Time dependent terms: //rho dd[idx] += base->vol[j] / dt ; r[idx_r] += base->vol[j] / dt * u[j].u1; //momentum dd[idx + 4] += base->vol[j] / dt ; r[idx_r+ 1] += base->vol[j] / dt * u[j].u2; //energy dd[idx + 8] += base->vol[j] / dt ; r[idx_r+ 2] += base->vol[j] / dt * u[j].u3; // divergence terms //double D = 1. * base->surf[j]; //uu[idx] = 0; //-D ; //dd[idx] += 0 ; //dd[idx+stride] = 0 ; //ll[idx+stride] = 0;//-D ; } cout<<"Boundaries"<<endl; // More Boundaries: // Left boundary: // Reflecting / no flux or planetary temperature for (int j=0; j < base->num_ghosts; j++) { int idx = j*stride ; int idx_r = j*num_vars ; //ll[idx] = 0 ; //uu[idx] = -dd[idx] ; //r[idx_r] = 0 ; } cout<<" dd and r "<<endl; for (auto i: dd) std::cout << i << ' '; cout<<endl; for (auto i: r) std::cout << i << ' '; cout<<endl; cout<<"Solving"<<endl; // // Solve! // base->implicit_tridiag.factor_matrix(&ll[0], &dd[0], &uu[0]) ; base->implicit_tridiag.solve(&r[0], &r[0]) ; // Solve in place // // End Solve // cout<<"Writing"<<endl; // // Write solution back into variables // for (int j=0; j <= base->num_cells; j++) { cout<<" j = "<<j<<" old/new rho = "<<u[j].u1<<" / "<<r[j*num_vars]<<endl; cout<<" j = "<<j<<" old/new mom = "<<u[j].u2<<" / "<<r[j*num_vars + 1]<<endl; cout<<" j = "<<j<<" old/new E = "<<u[j].u3<<" / "<<r[j*num_vars + 2]<<endl; u[j].u1 = r[j*num_vars] ; u[j].u2 = r[j*num_vars + 1] ; u[j].u3 = r[j*num_vars + 2] ; } // Update energies. eos->compute_primitive(&(u[0]), &(prim[0]), base->num_cells+2) ; eos->compute_auxillary(&(prim[0]), base->num_cells+2); // species[si].eos->compute_conserved(&(species[si].prim[0]), &(species[si].u[0]), num_cells+2); } "

 In its application, this code is still underdevelopped, as it only implements "d\rho/dt=0", "d \momentum/dt=0", and " d Energy/dt = 0", here small "d/dt" indicates the partial derivative w.r.t time t. Can you help me to implement simple advection operators, for all three variables so that I can solve the equations "D\rho/dt=0", "D \momentum/dt=0", and "D Energy/dt = 0", where captical "D/Dt" stands for the lagrangian derivative, i.e. "D/Dt = d/dt + v * d/dx ". The velocity "v" is to be taken at the advanced, implicit timestep, and is related to rho and momentum by "v = momentum/rho". In order to implement those advection operators, the right fluxes in each quantity rho, momentum and Energy have to be taken, assume they are called f_rho, f_mom, and f_E, and assigned into the right index positions of the matrices ll,dd,uu and the vector r.
